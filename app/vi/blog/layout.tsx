// Vietnamese blog segment — loads the same v2 design system sheet so the
// header/footer and pixel fonts (incl. DearPix for VI) match the site.
import '../../v2/fonts.css'

export default function ViBlogLayout({ children }: { children: React.ReactNode }) {
  return children
}
